using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 作成途中
/// PlayerControllerの共通部分とGicoの独自部分を切り分け、独自部分を本スクリプトに移す予定
/// </summary>
public class GicoController : PlayerController
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
